# buildspec.yml
version: 0.2

phases:
  install:
    runtime-versions:
      docker: 18
  build:
    commands:
      - echo "Setting up build environment"
      - bin/setup.sh
      - echo "Building the Kolla image(s)"
      - bin/kolla-build.sh $IMAGE_REPO_NAME
      - bin/ecr-push.sh
